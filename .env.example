# Local dev config for vistaar-dashboard (copy to .env). Build-time REACT_APP_*
# vars — public, baked into the static bundle. Production values come from the
# GitLab File variable APP_ENV at deploy (written to .env.production.local).
PORT=5173
REACT_APP_API_BASE=http://localhost:3001
REACT_APP_GOOGLE_CLIENT_ID=645389263058-e7ac4jub7j2uk744v154f1ft0dpbfo1s.apps.googleusercontent.com
